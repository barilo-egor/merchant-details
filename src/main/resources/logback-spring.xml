<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Паттерны -->
    <property name="CONSOLE_PATTERN" value="%d{HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{36}) - %msg%n"/>
    <property name="FILE_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- 1. КОНСОЛЬ (все логи) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!-- 2. ОСНОВНОЙ ФАЙЛ (все логи) -->
    <appender name="ALL_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/all.log</file>
        <encoder>
            <pattern>${FILE_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/all.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>3</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- 3. ДИНАМИЧЕСКИЙ РАСПРЕДЕЛИТЕЛЬ (SIFTING) -->
    <appender name="APPLICATION_SIFTER" class="ch.qos.logback.classic.sift.SiftingAppender">
        <discriminator>
            <key>logDest</key>
            <defaultValue>bot</defaultValue>
        </discriminator>
        <sift>
            <appender name="FILE-${logDest}" class="ch.qos.logback.core.rolling.RollingFileAppender">
                <file>logs/application-${logDest}.log</file>
                <encoder>
                    <pattern>${FILE_PATTERN}</pattern>
                    <charset>utf8</charset>
                </encoder>
                <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                    <fileNamePattern>logs/application-${logDest}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                    <maxFileSize>50MB</maxFileSize>
                    <maxHistory>3</maxHistory>
                    <totalSizeCap>3GB</totalSizeCap>
                </rollingPolicy>
            </appender>
        </sift>
    </appender>

    <!-- 4. SPRING ФАЙЛ -->
    <appender name="SPRING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/spring.log</file>
        <encoder>
            <pattern>${FILE_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/spring.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>3</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- НАСТРОЙКИ ЛОГГЕРОВ -->

    <logger name="tgb.cryptoexchange" additivity="false" level="DEBUG">
        <appender-ref ref="APPLICATION_SIFTER"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <logger name="org.springframework" additivity="false">
        <appender-ref ref="SPRING_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="ALL_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
